name: CD - Dev to Main

on:
    push:
        branches:
            - main

jobs:
    publish-dev:
        uses: ./.github/workflows/reusable-build-publish.yml
        with:
            image_tag: dev
        secrets: inherit