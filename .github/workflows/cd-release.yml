name: CD - Release

on:
    release:
        types: [published]
    workflow_dispatch:

jobs:
    publish-release:
        uses: ./.github/workflows/reusable-build-publish.yml
        with:
            image_tag: ${{ github.event.release.tag_name }}
            push_latest: true
        secrets: inherit